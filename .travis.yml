language: rust
rust:
  - nightly

before_script:
  - rustup component add rustfmt-preview --toolchain nightly

script:
  - make check
  - cargo test
  - make test
  - make test-bottom

notifications:
  email:
    on_success: never
    on_failure: never
